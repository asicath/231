{
  "name": "web",
  "version": "1.0.0",
  "description": "",
  "main": "jquery-1.9.1.min.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build-data && npm run build-web",
    "build-web": "npx webpack --config webpack.config.js --mode development",
    "build-data": "cd gates && node process-gates.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.6.1",
    "webpack": "^5.75.0"
  },
  "devDependencies": {
    "webpack-cli": "^4.10.0"
  }
}
